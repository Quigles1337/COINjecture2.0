[workspace]
resolver = "2"

members = [
    "adzdb",
    "core",
    "consensus",
    "network",
    "state",
    "mempool",
    "rpc",
    "tokenomics",
    "node",
    "wallet",
    "marketplace-export",
    "huggingface",
    "mobile-sdk",
    "load-test",
    "api-server",
]

[workspace.package]
authors = ["Quigles1337 <adz@alphx.io>"]
edition = "2021"
license = "MIT"
repository = "https://github.com/Quigles1337/COINjecture2.0"
version = "4.8.4"

[workspace.dependencies]
# Async runtime
tokio = { version = "1.41", features = ["full"] }
async-trait = "0.1"
futures = "0.3"

# Networking - CPP (COINjecture P2P Protocol) uses only std lib TCP
# libp2p removed - see network/src/cpp/ for the custom protocol

# Cryptography
sha2 = "0.10"
sha3 = "0.10"
blake3 = "1.5"
ed25519-dalek = { version = "2.1", features = ["rand_core"] }
x25519-dalek = "2.0"
chacha20poly1305 = "0.10"
rand = "0.9"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bincode = "1.3"

# Database (production-grade, pure Rust, ACID-compliant, actively maintained)
redb = "2.1"

# Math & Problem Solving
num-bigint = "0.4"
num-rational = "0.4"
num-traits = "0.2"

# Caches
lru = "0.12"

# Codec
tokio-util = { version = "0.7", features = ["codec"] }

# Encryption & Key Management (P0 security)
aes-gcm = "0.10"
argon2 = "0.5"
zeroize = { version = "1.8", features = ["derive"] }
subtle = "2.5"

# Utilities
thiserror = "2.0"
anyhow = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json", "fmt"] }
tracing-appender = "0.2"
clap = { version = "4.5", features = ["derive"] }
hex = "0.4"
bs58 = "0.5"

# Time
chrono = "0.4"

# Testing
proptest = "1"
tempfile = "3"

# RPC
jsonrpsee = { version = "0.24", features = ["server", "client", "macros"] }

# Metrics
prometheus = { version = "0.13", features = ["process"] }
lazy_static = "1.4"
hyper = { version = "1.5", features = ["full"] }
hyper-util = { version = "0.1", features = ["tokio"] }
http-body-util = "0.1"
http-body = "1.0"
http = "1.0"
bytes = "1.5"
dashmap = "6"
rustls = { version = "0.23", features = ["ring"] }
tokio-rustls = "0.26"
rustls-pemfile = "2"

# Benchmarking
criterion = { version = "0.5", features = ["html_reports"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
